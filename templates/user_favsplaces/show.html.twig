{% extends 'base.html.twig' %}

{% block title %}UserFavsplaces{% endblock %}

{% block body %}
    <h1>UserFavsplaces</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdFavs</th>
                <td>{{ user_favsplace.idFavs }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_favsplaces_index') }}">back to list</a>

    <a href="{{ path('app_user_favsplaces_edit', {'idFavs': user_favsplace.idFavs}) }}">edit</a>

    {{ include('user_favsplaces/_delete_form.html.twig') }}
{% endblock %}

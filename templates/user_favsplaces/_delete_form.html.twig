<form method="post" action="{{ path('app_user_favsplaces_delete', {'idFavs': user_favsplace.idFavs}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_favsplace.idFavs) }}">
    <button class="btn">Delete</button>
</form>

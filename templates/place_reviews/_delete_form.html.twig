<form method="post" action="{{ path('app_place_reviews_delete', {'reviewId': place_review.reviewId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ place_review.reviewId) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_placetovisit_delete', {'placeId': placetovisit.placeId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ placetovisit.placeId) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Edit Placetovisit{% endblock %}

{% block body %}
    <h1>Edit Placetovisit</h1>

    {{ include('placetovisit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_placetovisit_index') }}">back to list</a>

    {{ include('placetovisit/_delete_form.html.twig') }}
{% endblock %}
